
    # # Generate PDF from template
    # template_path = 'pdffiles/temp'  # Provide the path to your template PDF file
    # generator = GenerateFromTemplate(template_path)
    # generator.addText(replacement_name, (inst[0], inst[1]))  # Add the replacement name at the same location
    # generator.merge()
    # generator.generate(new_pdf_path)
